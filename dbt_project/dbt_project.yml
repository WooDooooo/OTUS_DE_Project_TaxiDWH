name: nyctaxi
version: '1.0.0'
config-version: 2

profile: nyctaxi

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  nyctaxi:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
